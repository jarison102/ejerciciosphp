<?php 

/*
$Canciones = [2,3,4,5,6,7,8,9,10];
foreach($Canciones as $Lista){
    echo ($Lista);
}
for ($Canciones = 0; $Canciones <= 10; $Canciones++){
    echo ($Canciones);
}
*/


/*
function Numeros(){
    if($Palabra = $_POST ['Palabra']){
        $Palabra = $_POST ['Palabra'];
        if($Palabra == $Palabra){
            for($i = 0; $i <= 10; $i++){
                echo $Palabra . "<br>";
            }
        }

    }

}
Numeros()
*/
/*
function Años(){
            if($Años = $_POST ['Años']){
                $Años = $_POST['Años']; // Obtén el valor ingresado en el formulario
                for($i = $Años; $i <= 10; $i++){
                    echo $i;
                }
                }
        }
    

Años()
*/

/*
function Calcular(){
    if($Numero = $_POST ['Numero']){
        $Numero = intval($_POST['Numero']); // Convertir el valor en un número entero
        // Verificar que el número sea positivo antes de realizar la cuenta atrás
        if ($Numero >= 0) {
            for ($i = $Numero; $i >= 0; $i--) {
                echo $i . ($i > 0 ? ', ' : ''); // Imprimir número y coma si no es cero
            }
        } else {
            echo "Por favor, ingresa un número entero positivo.";
        }
    }
}
Calcular();
*/

?>


